{
  "_test": {
    "timeout_s": 2400,
    "setup": "mkdir -p $HOME/pw/tests/rag-docs && printf '%s\\n' '# Zephyr cluster runbook' 'The zephyr scratch quota is 42 terabytes per project.' > $HOME/pw/tests/rag-docs/zephyr.md && printf '%s\\n' '# Tarragon protocol' 'The tarragon handshake requires exactly nine retries and listens on port 7777.' > $HOME/pw/tests/rag-docs/tarragon.md",
    "warm_marker": [
      "${HOME}/pw/software/containers/rag-service.sif",
      "${HOME}/pw/software/models"
    ],
    "leftover_patterns": [
      "rag_server",
      "pw endpoints run"
    ]
  },
  "cluster": {
    "resource": "pw://alvaro/gcpsmall",
    "scheduler": false
  },
  "service": {
    "docs_dir": "${HOME}/pw/tests/rag-docs",
    "embedding_model_id": "BAAI/bge-small-en-v1.5"
  }
}
