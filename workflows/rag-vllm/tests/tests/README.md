# Workflow Config Tests

These tests validate the YAML workflow configuration files and key defaults.

Run:

```bash
./tests/run.sh
```

Or directly:

```bash
python3 -m unittest discover -s tests -p "test_*.py"
```
